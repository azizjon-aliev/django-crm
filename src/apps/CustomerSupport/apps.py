from django.apps import AppConfig


class CustomerSupportConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "src.apps.CustomerSupport"
    verbose_name = "Поддержка клиентов"
